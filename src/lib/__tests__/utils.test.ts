import { describe, it, expect } from 'vitest'

// Ejemplo de tests para funciones utilitarias
// Por ahora utils.ts está vacío, pero este archivo está listo para cuando agregues funciones

describe('utils', () => {
  it('debería tener tests cuando se agreguen funciones utilitarias', () => {
    expect(true).toBe(true)
  })
})

